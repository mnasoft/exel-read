(defpackage exel-read-test
  (:use :cl
        :exel-read
        :prove))
(in-package :exel-read-test)

;; NOTE: To run this test file, execute `(asdf:test-system :exel-read)' in your Lisp.

(plan nil)

;; blah blah blah.

(finalize)
